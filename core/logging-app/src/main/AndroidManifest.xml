<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Keep synchronized with CallerValidator.DefaultAllowedPackages. These queries let the
         module app grant reverse package visibility only to installed Hook targets. -->
    <queries>
        <package android:name="com.google.android.apps.photos" />
        <package android:name="com.google.android.inputmethod.latin" />
        <package android:name="com.baidu.input" />
        <package android:name="com.baidu.input_mi" />
        <package android:name="com.sohu.inputmethod.sogou" />
        <package android:name="com.sohu.inputmethod.sogou.xiaomi" />
        <package android:name="com.iflytek.inputmethod" />
        <package android:name="com.iflytek.inputmethod.miui" />
        <package android:name="com.tencent.wetype" />
        <package android:name="keepass2android.keepass2android" />
    </queries>

    <application>
        <!-- URI grants establish Android 11+ reverse package visibility for scoped Hook targets.
             Provider calls remain restricted by protocol, UID and package validation. -->
        <provider
            android:name="io.github.togls.hypertweaks.logging.app.ingest.LogIngestProvider"
            android:authorities="io.github.togls.hypertweaks.log.ingest"
            android:exported="true"
            android:grantUriPermissions="true"
            tools:ignore="ExportedContentProvider" />
    </application>
</manifest>
